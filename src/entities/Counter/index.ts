import { counterReducer } from './ui/model/slice/CounterSlice';
import { Counter } from './ui/Counter';
import type { CounterSchema } from './ui/model/types/counterSchema';

export {
    counterReducer,
    Counter,
    CounterSchema,
};
